---
import { Image } from 'astro:assets';

interface Props {
  src: string;
  alt: string;
  class?: string;
}

const { src, alt, class: className } = Astro.props;
---

<Image
  src={src}
  alt={alt}
  width={800}
  height={600}
  class={className}
  loading="lazy"
  decoding="async"
/> 