---
import { Image } from 'astro:assets';
// Ajusta aquesta ruta segons on tinguis realment les imatges
import installationsImage from '/public/assets/la-jetee-films-instalaciones-home.webp';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<Image
  src={installationsImage}
  alt="Installations - La Jetée Films"
  width={800}
  height={400}
  class={className}
  loading="lazy"
  decoding="async"
/> 