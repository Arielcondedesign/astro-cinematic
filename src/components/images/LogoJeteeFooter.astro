---
import { Image } from 'astro:assets';
import logoSrc from '../../assets/lajetee-brand.svg';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<Image
  src={logoSrc}
  alt="La Jetée Films"
  width={124}
  height={18}
  class={className}
  loading="lazy"
  decoding="async"
  format="svg"
/> 